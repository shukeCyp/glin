name: Build & Package

on:
  push:
    branches: ["**"]

jobs:
  build-windows:
    runs-on: windows-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Set up Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install Python dependencies
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt pyinstaller

      - name: Build frontend
        working-directory: frontend
        run: |
          npm ci
          npm run build

      - name: Package main app
        run: |
          pyinstaller --name "Glin带货神器" --onefile --windowed --add-data "static;static" main.py

      - name: Package auth tool
        run: |
          pyinstaller --name "Glin授权码" --onefile --windowed generate_code.py

      - name: Upload Windows artifacts
        uses: actions/upload-artifact@v4
        with:
          name: Windows
          path: |
            dist/Glin带货神器.exe
            dist/Glin授权码.exe
          compression-level: 0

  build-macos:
    runs-on: macos-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Set up Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install Python dependencies
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt pyinstaller

      - name: Build frontend
        working-directory: frontend
        run: |
          npm ci
          npm run build

      - name: Package main app
        run: |
          pyinstaller --name "Glin带货神器" --onefile --windowed --add-data "static:static" main.py

      - name: Package auth tool
        run: |
          pyinstaller --name "Glin授权码" --onefile --windowed generate_code.py

      - name: Create main app DMG
        run: |
          mkdir -p dmg_main
          if [ -d "dist/Glin带货神器.app" ]; then
            cp -r "dist/Glin带货神器.app" dmg_main/
          elif [ -f "dist/Glin带货神器" ]; then
            cp "dist/Glin带货神器" dmg_main/
          fi
          hdiutil create -volname "Glin带货神器" -srcfolder dmg_main -ov -format UDZO "dist/Glin带货神器.dmg"

      - name: Create auth tool DMG
        run: |
          mkdir -p dmg_auth
          if [ -d "dist/Glin授权码.app" ]; then
            cp -r "dist/Glin授权码.app" dmg_auth/
          elif [ -f "dist/Glin授权码" ]; then
            cp "dist/Glin授权码" dmg_auth/
          fi
          hdiutil create -volname "Glin授权码" -srcfolder dmg_auth -ov -format UDZO "dist/Glin授权码.dmg"

      - name: Upload macOS artifacts
        uses: actions/upload-artifact@v4
        with:
          name: macOS
          path: |
            dist/Glin带货神器.dmg
            dist/Glin授权码.dmg
          compression-level: 0
